import Typography from '@material-ui/core/Typography';
import React from 'react';

const broadcastPost = (user, post, wif) => {
  // broadcasting
};

const broadcastAccountUpdate = (user, accountMetadata, wif) => {
  // broadcasting
};

const AccountSetup = props => {
  return (
    <Typography>
      Please wait while your account is being generated... (Broadcast the
      transactions) Your account has been created! You can now log in.
    </Typography>
  );
};

export default AccountSetup;
